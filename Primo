#include <stdio.h>

int resultado(int n);

int main() {
    int n,b;
    scanf("%d",&n);
    b=resultado(n);
    if (b==1)
        printf("Nao Primo\n");
    else
        printf("Primo\n");
}
int resultado(int n){
    int cont,b;
    for(int c=1;c<n;c++){
        if(n%c==0)
             cont+=1;
    }
if(cont>1){
   b=1;
}
return b;
}
